# 模板修复总结

生成时间: 2025-12-04

## 已完成的修复

### 1. ✅ 创建统一规范文档
- 文件：`template_unified_specification.md`
- 内容：统一输出格式、置信度格式、参数名称、步骤类型等规范

### 2. ✅ 统一参数名称
- `{question}` → `{query}` (2处)
  - `general_query` 模板
  - `multi_hop_reasoning_type_classification` 模板

### 3. ✅ 创建修复计划
- 文件：`template_fix_plan.md`
- 内容：分阶段修复策略和优先级

## 待实施的修复

### 方案选择

由于核心模板（`reasoning_with_evidence`, `reasoning_without_evidence`）内容非常长且正在使用中，建议采用**渐进式修复**策略：

#### 推荐方案：代码层面统一处理（方案C）

**优势**：
- ✅ 保持向后兼容
- ✅ 不破坏现有功能
- ✅ 可以逐步迁移
- ✅ 降低风险

**实施内容**：
1. 在 `src/core/reasoning/answer_extractor.py` 中添加格式转换函数
2. 统一处理置信度格式转换（`high/medium/low` ↔ `0.0-1.0`）
3. 统一处理 final_answer 格式提取
4. 添加 metadata 字段生成

#### 备选方案：创建新模板（方案B）

**优势**：
- ✅ 可以完全按照新规范设计
- ✅ 保留旧模板作为fallback

**劣势**：
- ⚠️ 需要维护两套模板
- ⚠️ 需要更新模板选择逻辑

## 关键问题解决状态

| 问题 | 状态 | 方案 |
|------|------|------|
| 参数名称不一致 | ✅ 部分修复 | 已统一 `{question}` → `{query}` |
| 置信度格式不一致 | 🔄 待实施 | 代码层面统一转换 |
| Final Answer格式 | ✅ 已统一 | 所有模板都使用 `---\nFINAL ANSWER:\n---` |
| 步骤类型过多 | 🔄 待实施 | 简化步骤类型系统 |
| 证据质量评估缺失 | 🔄 待实施 | 创建新模板 |
| 模板间指令冲突 | 🔄 待实施 | 统一规范文档已创建 |

## 下一步行动

1. **立即行动**（已完成）：
   - ✅ 创建统一规范文档
   - ✅ 统一参数名称

2. **短期行动**（1-2天）：
   - 在代码层面添加格式转换函数
   - 统一处理置信度格式
   - 创建证据质量评估模板

3. **中期行动**（3-5天）：
   - 实现模板选择器
   - 建立置信度校准系统
   - 简化步骤类型系统

4. **长期行动**（持续）：
   - 监控模板使用情况
   - 持续优化和调整
   - 收集反馈并改进

---

**修复状态**: 第一阶段完成，等待进一步指示

