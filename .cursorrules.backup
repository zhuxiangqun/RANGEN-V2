# .cursorrules
context:
  - type: global_instruction
    content: |
      # 编程原则与规范

      ## 核心原则
      1.  **DRY (Don't Repeat Yourself)**: 禁止重复代码，必须提取为函数或公共模块。
      2.  **KISS (Keep It Simple, Stupid)**: 优先选择简单、清晰的实现，避免过度设计。
      3.  **单一职责原则**: 每个函数/类只做一件事，并做好。
      4.  **优先使用组合而非继承**。

      ## 安全原则
      1.  所有用户输入都必须经过验证和清理。
      2.  避免使用已知不安全的函数（如 `eval`）。

      ## 本项目特定规范
      1、尽量不要生成新的文件、模块跟方法，尽量使用统一中心系统
      2、以解决核心功能为主要目的，任何回退方法、回避方法等都不是根本的解决方法
      3、注意不要产生新的硬编码
      4、修改后检查有无pylint、pyright错误

      ## 代码风格
      1.  遵循 `rustfmt` 标准。
      2.  变量和函数名使用 `snake_case`，类型名使用 `PascalCase`。

      ## 指令
      -   在编写、审查或重构代码时，必须严格遵守以上所有原则。
      -   如果用户请求违反了任何原则，你必须指出违反了哪条原则并解释原因，然后提供符合原则的替代方案。
