# 完善后的系统架构优化与重构方案

感谢您提供的宝贵补充！您的建议极具实操性，特别是对 **LangGraph** 的具体应用、**重工具拆分** 以及 **并行迁移策略**，完美补全了原方案的实施细节。

结合您的输入，我已将方案升级为 **增强版重构路线图 (Enhanced Refactoring Roadmap)**。

---

## 🏗️ 核心架构升级点 (Key Upgrades)

1.  **编排引擎 (Orchestration)**: 明确采用 **LangGraph** 实现状态机，替代复杂的 `if-else` 逻辑。
2.  **工具粒度 (Tool Granularity)**: 强制 **原子化 (Atomicity)**，将大而全的 `RAGTool` 拆解为单一职责的小工具。
3.  **平滑迁移 (Smooth Migration)**: 引入 **MigrationProxy** 实现双轨运行与数据对比，确保零风险切换。

---

## 📅 详细实施计划 (Detailed Implementation Plan)

### Phase 1: 瘦身与定义 (Cleaning & Defining)
**目标**: 明确新旧边界，止血（停止增加新债）。

1.  **接口定义 (已完成 ✅)**:
    *   `src/interfaces/architecture_interfaces.py`: 定义了 `IRouter`, `ICoordinator`, `IAgent`。
2.  **组件映射与废弃 (Component Mapping & Deprecation)**:
    *   **动作**: 扫描代码库，找到 `IntelligentRouter`, `AdvancedContextManager` 等类。
    *   **实施**: 添加 `@deprecated` 装饰器和 `DeprecationWarning`，并建立 `DEPRECATION.md` 记录迁移对应关系。

### Phase 2: 核心重组 (Core Refactoring) - **重点攻坚**
**目标**: 构建基于 LangGraph 的新内核。

1.  **实现 Coordinator (LangGraph)**:
    *   **动作**: 创建 `src/core/coordinator/execution_coordinator.py`。
    *   **细节**: 使用 `StateGraph` 定义 `Reason -> Act -> Observe` 循环。
    *   **状态**: 维护 `AgentState` (messages, current_step, tools_output)。

2.  **重构 Agent 基类**:
    *   **动作**: 创建 `src/agents/base/modern_agent.py` (暂定名，区分于旧 BaseAgent)。
    *   **细节**: 实现 `execute(task, context)` 接口，剥离工具执行逻辑，只负责 Prompting 和 Decision。

3.  **工具原子化拆分**:
    *   **动作**: 拆解 `src/agents/tools/rag_tool.py`。
    *   **产出**: `RetrievalTool`, `RerankTool`, `GenerationTool`。
    *   **注册**: 更新 `ToolRegistry` 以支持新工具。

4.  **统一 Context Manager**:
    *   **动作**: 实现单例 `src/core/context/unified_context.py`。
    *   **细节**: 统一 Session 管理，确保新旧系统都能访问。

### Phase 3: 迁移与切换 (Migration & Switch)
**目标**: 无感切换，数据验证。

1.  **实现 MigrationProxy**:
    *   **动作**: 创建 `src/core/migration_proxy.py`。
    *   **逻辑**: 同时调用 Old Router 和 New Router，记录结果差异到日志/看板。
2.  **双轨运行 (Shadow Mode)**:
    *   **配置**: 在 `.env` 中添加 `ENABLE_SHADOW_MODE=true`。
    *   **验证**: 运行一周，收集 Metrics（成功率、延迟）。
3.  **正式切换**:
    *   **动作**: 修改入口，默认走新架构。保留旧代码 2 周作为备份。

---

## 🚀 立即执行行动 (Immediate Actions)

根据待办列表，我将立即开始 **Phase 1 的剩余任务** 和 **Phase 2 的准备工作**：

1.  **扫描并标记废弃类**: 我将遍历 `src/`，识别所有 "Adjective Inflation" 类，添加废弃警告。
2.  **创建组件映射文档**: 生成 `docs/architecture/COMPONENT_MAPPING.md`，明确新旧对应关系。
3.  **搭建 Coordinator 骨架**: 引入 `langgraph` (如果尚未安装需确认)，创建 Coordinator 的基础结构。

我已更新 Todo 列表，准备好开始执行。
