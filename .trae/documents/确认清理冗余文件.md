我已为您筛选出绝对安全的清理目标（总计约 **295MB**）。

### 🗑️ 清理执行计划
将删除以下路径的文件：
1.  `data/knowledge_management/backups/` 下所有 **2025年** 的旧备份及 **1月4日** 的备份。
2.  `data/faiss_memory_backup_1767606251`（临时文件）。
3.  `.mypy_cache/`（开发缓存）。

**注意**：`knowlege` 资料库和前端 `node_modules` **不会** 被触碰。

请确认是否执行此清理？